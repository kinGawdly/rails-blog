<nav class="flex justify-between p-3 bg-blue-900">
      <ul class="flex gap-2 items-center">
        <li class= "font-bold text-lg text-white"> <%= link_to 'Rails App', root_path %></li>
        <% if current_user && current_user.profile.present? %>
        <li> <%= link_to 'Articles', articles_path, class: "px-4 py-2 bg-red-500 text-white text-sm font-semibold rounded" %> </li>
        <li> <%= link_to 'Users', users_path, class: "camialita-btn" %> </li> 
        <li> <%= link_to 'Categories', categories_path %> </li>
        <li> <%= link_to 'New Article', articles_new_path %> </li>
        <li> <%= link_to 'Maquillajes', maquillaje_path(current_user.profile) %> </li>
        
        <% end %>
      </ul>

      <ul class="flex gap-2 items-center">
        <% if current_user %>
        <li> <%= button_to 'Logout', logout_path, method: :delete %></li>
        <li> <%= current_user.email %> </li>
        
      <% else %>
        <li> <%= link_to 'Login', login_path %></li>
        <li> <%= link_to 'Sign Up', signup_path %></li>
        <% end %>
      </ul>
</nav>  