<!DOCTYPE html>
<html>
  <head>
    <title>TempApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
   <%= render "shared/header" %> # Está línea incluye el encabezado compartido en todas las páginas
    </nav>  
    <%= yield %>
  </body>
</html>
